# Dependencies used by rust-test Markdown examples.
rust-version = "1.98"

[dependencies]
insta = { features = [ "json", "redactions", "yaml" ], version = "1" }
rstest = "0.27"
serde = { features = [ "derive" ], version = "1" }
