interface:
  display_name: "Manage Workspace AGENTS.md"
  short_description: "Explicit-only AGENTS.md creation, patching, and review"
  default_prompt: "Use $manage-workspace-agents-md for my explicitly requested AGENTS.md task, using repository evidence and maintainer requirements. Ask for the task or scope if missing. Keep review modes read-only; apply edits only in an authorized Draft or Patch."

policy:
  allow_implicit_invocation: false
