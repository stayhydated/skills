interface:
  display_name: "Use Windows VM Computer Use"
  short_description: "Run VM-local GUI control with forced full access"
  default_prompt: "Use $use-windows-vm-computer-use to run Codex computer-use with mandatory --yolo full access inside an interactive Windows VM session."
